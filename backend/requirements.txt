fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
openai>=1.50,<2
pydantic>=2.7,<3
Pillow>=10.0,<13
pdf2image>=1.17,<2
# Optional fallback when Poppler is unavailable; LibreOffice is required for DOCX/PPTX.
PyMuPDF>=1.24,<2
python-multipart>=0.0.9,<1
# Optional: set SEMANTIC_CACHE_BACKEND=redis and REDIS_URL to use Redis.
redis>=5.0,<6
